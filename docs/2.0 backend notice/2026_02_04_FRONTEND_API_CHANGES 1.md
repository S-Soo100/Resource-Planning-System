# 프론트엔드 API 변경사항 안내

> **백엔드 배포 예정일**: TBD
> **하위 호환성**: ✅ 모든 변경사항 하위 호환 (기존 코드 수정 없이 동작)

---

## 📋 변경 요약

| 기능             | API       | 변경 내용              | 필수 대응 |
| ---------------- | --------- | ---------------------- | --------- |
| 장기시연 구분    | Demo      | `isLongTerm` 필드 추가 | ❌ 선택   |
| 창고 순서        | Warehouse | `sortOrder` 필드 추가  | ❌ 선택   |
| 아이템 입고 기록 | Item      | 내부 로직 변경         | ❌ 없음   |

---

## 1. 장기시연 구분 (`isLongTerm`)

### 개요

시연(Demo)을 일반 시연과 장기시연으로 구분하는 필드가 추가됩니다.

### API 변경사항

#### 1.1 시연 생성 (POST /demo)

**Request Body 추가 필드:**

```json
{
  "requester": "홍길동",
  "demoTitle": "신제품 시연",
  // ... 기존 필드들 ...
  "isLongTerm": true // 추가 (선택)
}
```

| 필드         | 타입      | 필수 | 기본값  | 설명          |
| ------------ | --------- | ---- | ------- | ------------- |
| `isLongTerm` | `boolean` | ❌   | `false` | 장기시연 여부 |

#### 1.2 시연 수정 (PATCH /demo/:id)

```json
{
  "isLongTerm": true // 추가 (선택)
}
```

#### 1.3 시연 조회 응답 (GET /demo, GET /demo/:id)

**Response 추가 필드:**

```json
{
  "id": 1,
  "requester": "홍길동",
  "demoTitle": "신제품 시연",
  // ... 기존 필드들 ...
  "isLongTerm": false // 추가
}
```

---

## 2. 창고 순서 (`sortOrder`)

### 개요

팀별 창고 목록의 정렬 순서를 지정할 수 있습니다.

### 정렬 규칙

| sortOrder 값    | 표시 순서               |
| --------------- | ----------------------- |
| `0`             | 첫 번째                 |
| `1`             | 두 번째                 |
| `2`             | 세 번째                 |
| `null` (미지정) | **맨 아래** (생성일 순) |

### API 변경사항

#### 2.1 창고 생성 (POST /warehouse)

**Request Body 추가 필드:**

```json
{
  "warehouseName": "서울 창고",
  "warehouseAddress": "서울시 강남구",
  "teamId": 1,
  "sortOrder": 0 // 추가 (선택)
}
```

| 필드        | 타입             | 필수 | 기본값 | 설명                                   |
| ----------- | ---------------- | ---- | ------ | -------------------------------------- |
| `sortOrder` | `number \| null` | ❌   | `null` | 정렬 순서 (0부터 시작, null은 맨 아래) |

**유효성 검사:**

- 최소값: `0`
- `null` 또는 미전송: 맨 아래 표시

#### 2.2 창고 수정 (PATCH /warehouse/:id)

```json
{
  "sortOrder": 1 // 순서 변경
}
```

```json
{
  "sortOrder": null // 순서 해제 (맨 아래로 이동)
}
```

**자동 재정렬:**

- 같은 순서가 이미 있으면 기존 항목들이 자동으로 +1 밀려남
- 예: `sortOrder: 1`로 변경 시, 기존 1번 이후 항목들이 2, 3, 4...로 변경

#### 2.3 창고 조회 응답 (GET /warehouse)

**Response 추가 필드:**

```json
[
  {
    "id": 1,
    "warehouseName": "본사 창고",
    "sortOrder": 0 // 추가
  },
  {
    "id": 2,
    "warehouseName": "서울 창고",
    "sortOrder": 1 // 추가
  },
  {
    "id": 3,
    "warehouseName": "기존 창고",
    "sortOrder": null // 미지정 (맨 아래)
  }
]
```

> **참고**: 응답은 이미 `sortOrder` 기준으로 정렬되어 반환됩니다.

---

## 3. 아이템 입고 기록 자동 생성

### 개요

아이템 생성 시 입고 기록이 자동으로 생성됩니다.

### 프론트엔드 영향

**없음** - 백엔드 내부 로직 변경이므로 API 인터페이스 변경 없음.

### 변경 내용

- 아이템 생성 시 초기 수량에 대한 입고 기록 자동 생성
- 입고 기록 목록에서 "아이템 초기 등록" 비고로 표시됨

---

## 📊 타입 정의 (TypeScript)

### Demo 타입 변경

```typescript
// 기존 CreateDemoDto에 추가
interface CreateDemoDto {
  // ... 기존 필드들 ...
  isLongTerm?: boolean; // 추가
}

// 기존 UpdateDemoDto에 추가
interface UpdateDemoDto {
  // ... 기존 필드들 ...
  isLongTerm?: boolean; // 추가
}

// 기존 DemoResponse에 추가
interface DemoResponse {
  // ... 기존 필드들 ...
  isLongTerm: boolean; // 추가
}
```

### Warehouse 타입 변경

```typescript
// 기존 CreateWarehouseDto에 추가
interface CreateWarehouseDto {
  warehouseName: string;
  warehouseAddress?: string;
  teamId: number;
  sortOrder?: number | null; // 추가
}

// 기존 UpdateWarehouseDto에 추가
interface UpdateWarehouseDto {
  warehouseName?: string;
  warehouseAddress?: string;
  teamId?: number;
  sortOrder?: number | null; // 추가
}

// 기존 WarehouseResponse에 추가
interface WarehouseResponse {
  id: number;
  warehouseName: string;
  warehouseAddress: string | null;
  teamId: number;
  sortOrder: number | null; // 추가
  createdAt: string;
  updatedAt: string;
}
```

---

## ❓ FAQ

### Q: 기존 코드를 수정해야 하나요?

**A**: 아니요. 모든 변경사항이 하위 호환됩니다. 새 필드를 사용하지 않아도 기존 기능은 정상 동작합니다.

### Q: isLongTerm을 보내지 않으면 어떻게 되나요?

**A**: 기본값 `false`로 설정됩니다 (일반 시연).

### Q: sortOrder를 보내지 않으면 어떻게 되나요?

**A**: `null`로 설정되어 창고 목록 맨 아래에 표시됩니다.

### Q: 창고 순서를 변경하면 다른 창고 순서도 바뀌나요?

**A**: 네. 같은 순서가 있으면 기존 항목들이 자동으로 +1 밀려납니다.

### Q: 기존 창고들의 sortOrder는 어떻게 되나요?

**A**: 모두 `null`로 설정되어 기존 생성일 순서가 유지됩니다.

---
