# 변경 이력 표시 시스템 기획서

> **버전**: 1.0
> **작성일**: 2025-12-22
> **대상**: 프론트엔드 개발자
> **관련 문서**: [CHANGE_HISTORY_API 1.md](./CHANGE_HISTORY_API%201.md)

---

## 목차

1. [개요](#개요)
2. [설계 방향](#설계-방향)
3. [페이지별 구현 계획](#페이지별-구현-계획)
4. [컴포넌트 구조](#컴포넌트-구조)
5. [API 통신](#api-통신)
6. [UI/UX 상세](#uiux-상세)
7. [구현 우선순위](#구현-우선순위)
8. [디렉토리 구조](#디렉토리-구조)

---

## 개요

### 목적

Order(발주), Demo(시연), Item(재고) 상세 페이지에 변경 이력을 표시하여 사용자가 다음 정보를 확인할 수 있도록 함:

- 누가, 언제, 무엇을 변경했는지
- 변경 전/후 값 비교
- 상태 변경 이력 추적
- 재고 증감 내역 확인

### 핵심 원칙

1. **현재 디자인 패턴 유지**: 단일 스크롤 페이지 구조 유지
2. **점진적 공개**: 기본은 접혀있고, 필요시 펼침
3. **성능 최적화**: React Query 캐싱, 페이지네이션 사용
4. **일관성**: Order/Demo/Item 모두 동일한 UX 제공

---

## 설계 방향

### 옵션 비교

| 구분 | 옵션 A: 페이지 내 섹션 | 옵션 B: Ant Design Tabs |
|------|----------------------|------------------------|
| **일관성** | ✅ 현재 패턴 유지 | ❌ 전체 리팩토링 필요 |
| **접근성** | ✅ 스크롤로 모든 정보 확인 | ❌ 탭 이동 필요 |
| **구현 난이도** | ✅ 낮음 | ❌ 높음 |
| **페이지 길이** | ⚠️ 길어짐 (접기로 해결) | ✅ 짧음 |

### 선택: **옵션 A (페이지 내 섹션)** ⭐

현재 KARS의 디자인 패턴(댓글 섹션 포함)과 일관성을 유지하며, 구현 난이도가 낮고 사용자 경험이 자연스러움.

---

## 페이지별 구현 계획

### 1. Order 상세 페이지

**파일**: `src/app/orderRecord/[id]/page.tsx`

#### 섹션 배치 순서

```tsx
<div>
  {/* 기존 섹션들 */}
  <헤더 />
  <상태_정보 />
  <기본_정보_카드 />
  <배송_정보_카드 />
  <발주_품목_목록 />
  <추가_요청사항_메모 />

  {/* 🆕 변경 이력 섹션 */}
  <OrderChangeHistory orderId={orderId} />

  {/* 기존 섹션들 */}
  <첨부파일 />
  <CommentSection />
</div>
```

#### 추적 항목

- `create`: 발주 생성
- `update`: 필드 수정 (수령인, 배송 주소, 배송일 등)
- `status_change`: 상태 변경 (요청 → 승인 → 발송 완료 등)
- `delete`: 발주 삭제

---

### 2. Demo 상세 페이지

**파일**: `src/app/demoRecord/[id]/page.tsx`

#### 섹션 배치 순서

```tsx
<div>
  {/* 기존 섹션들 */}
  <헤더 />
  <상태_정보 />
  <기본_정보_카드 />
  <시연_정보_카드 />
  <시연_일정 />
  <시연품_목록 />
  <메모 />

  {/* 🆕 변경 이력 섹션 */}
  <DemoChangeHistory demoId={demoId} />

  {/* 기존 섹션들 */}
  <첨부파일 />
  <CommentSection />
</div>
```

#### 추적 항목

- `create`: 시연 생성
- `update`: 필드 수정 (시연 날짜, 주소, 담당자, 행사 일정 등)
- `status_change`: 상태 변경 (요청 → 승인 → 완료 등)
- `delete`: 시연 삭제

---

### 3. Item 상세 페이지

**파일**: `src/app/item/[id]/page.tsx`

#### 섹션 배치 순서

```tsx
<div>
  {/* 기존 섹션들 */}
  <품목_기본_정보_헤더 />
  <이미지_섹션 />
  <창고_정보 />
  <기본_정보 />

  {/* 🆕 재고 변동 이력 섹션 */}
  <ItemQuantityHistory itemId={itemId} />

  {/* 기존 섹션들 */}
  <입출고_내역_테이블 />
</div>
```

#### 추적 항목

- `quantity_change`: 재고 수량 변동 (증가/감소)

---

## 컴포넌트 구조

### 1. Order 변경 이력 컴포넌트

**파일**: `src/components/order/OrderChangeHistory.tsx`

```tsx
interface OrderChangeHistoryProps {
  orderId: number;
}

const OrderChangeHistory: React.FC<OrderChangeHistoryProps> = ({ orderId }) => {
  const [isExpanded, setIsExpanded] = useState(false); // 접기/펼치기
  const [page, setPage] = useState(1);
  const [actionFilter, setActionFilter] = useState<string | null>(null);

  const { data, isLoading } = useOrderChangeHistory(orderId, {
    page,
    limit: 10,
    action: actionFilter,
  });

  return (
    <div className="bg-white rounded-lg shadow p-6 mb-6">
      {/* 헤더 */}
      <div className="flex items-center justify-between mb-4">
        <h3 className="text-lg font-semibold flex items-center gap-2">
          <HistoryOutlined />
          변경 이력
          <span className="text-sm text-gray-500">
            (총 {data?.meta.total || 0}건)
          </span>
        </h3>

        <Button
          type="text"
          icon={isExpanded ? <UpOutlined /> : <DownOutlined />}
          onClick={() => setIsExpanded(!isExpanded)}
        >
          {isExpanded ? '접기' : '펼치기'}
        </Button>
      </div>

      {/* 필터 */}
      {isExpanded && (
        <div className="mb-4">
          <Radio.Group
            value={actionFilter}
            onChange={(e) => setActionFilter(e.target.value)}
          >
            <Radio.Button value={null}>전체</Radio.Button>
            <Radio.Button value="create">생성</Radio.Button>
            <Radio.Button value="update">수정</Radio.Button>
            <Radio.Button value="status_change">상태 변경</Radio.Button>
            <Radio.Button value="delete">삭제</Radio.Button>
          </Radio.Group>
        </div>
      )}

      {/* 타임라인 */}
      {isExpanded && (
        <Timeline
          pending={isLoading}
          items={data?.data.map((item) => ({
            color: getActionColor(item.action),
            dot: getActionIcon(item.action),
            children: <ChangeHistoryItem item={item} />,
          }))}
        />
      )}

      {/* 페이지네이션 */}
      {isExpanded && data && data.meta.totalPages > 1 && (
        <Pagination
          current={page}
          total={data.meta.total}
          pageSize={data.meta.limit}
          onChange={setPage}
          className="mt-4 text-center"
        />
      )}
    </div>
  );
};
```

#### 주요 기능

- **접기/펼치기**: 기본은 접혀있음
- **필터링**: 액션 타입별 필터 (전체/생성/수정/상태변경/삭제)
- **타임라인**: Ant Design Timeline 컴포넌트 사용
- **페이지네이션**: 10개씩 표시

---

### 2. Demo 변경 이력 컴포넌트

**파일**: `src/components/demo/DemoChangeHistory.tsx`

Order와 거의 동일한 구조. `useOrderChangeHistory` 대신 `useDemoChangeHistory` 훅 사용.

```tsx
interface DemoChangeHistoryProps {
  demoId: number;
}

const DemoChangeHistory: React.FC<DemoChangeHistoryProps> = ({ demoId }) => {
  // OrderChangeHistory와 동일한 로직
  const { data, isLoading } = useDemoChangeHistory(demoId, {
    page,
    limit: 10,
    action: actionFilter,
  });

  // ... 나머지 동일
};
```

---

### 3. Item 재고 변동 이력 컴포넌트

**파일**: `src/components/item/ItemQuantityHistory.tsx`

재고 변동에 특화된 UI 제공.

```tsx
interface ItemQuantityHistoryProps {
  itemId: number;
}

const ItemQuantityHistory: React.FC<ItemQuantityHistoryProps> = ({ itemId }) => {
  const [isExpanded, setIsExpanded] = useState(false);
  const [page, setPage] = useState(1);

  const { data, isLoading } = useItemQuantityHistory(itemId, {
    page,
    limit: 10,
  });

  return (
    <div className="bg-white rounded-lg shadow p-6 mb-6">
      <div className="flex items-center justify-between mb-4">
        <h3 className="text-lg font-semibold flex items-center gap-2">
          <StockOutlined />
          재고 변동 이력
          <span className="text-sm text-gray-500">
            (총 {data?.meta.total || 0}건)
          </span>
        </h3>

        <Button
          type="text"
          icon={isExpanded ? <UpOutlined /> : <DownOutlined />}
          onClick={() => setIsExpanded(!isExpanded)}
        >
          {isExpanded ? '접기' : '펼치기'}
        </Button>
      </div>

      {isExpanded && (
        <Timeline
          items={data?.data.map((item) => ({
            color: getQuantityChangeColor(item), // 증가: green, 감소: red
            children: (
              <div>
                {/* 증감 표시 */}
                <div className="flex items-center gap-2 mb-2">
                  {getQuantityChangeIcon(item)} {/* ▲ or ▼ */}
                  <span className={getQuantityChangeTextColor(item)}>
                    {formatQuantityChange(item)} {/* "+15개 (100 → 115)" */}
                  </span>
                  <span className="text-gray-500 text-sm">
                    {formatDateTime(item.createdAt)}
                  </span>
                </div>

                {/* 작업자 정보 */}
                <div className="flex items-center gap-2 text-sm text-gray-600">
                  <UserOutlined />
                  <span>{item.userName}</span>
                  <Tag color={getAccessLevelColor(item.accessLevel)}>
                    {getAccessLevelLabel(item.accessLevel)}
                  </Tag>
                </div>

                {/* 비고 */}
                {item.remarks && (
                  <div className="mt-2 text-sm text-gray-600 bg-gray-50 p-2 rounded">
                    💬 {item.remarks}
                  </div>
                )}
              </div>
            ),
          }))}
        />
      )}

      {/* 페이지네이션 */}
      {isExpanded && data && data.meta.totalPages > 1 && (
        <Pagination
          current={page}
          total={data.meta.total}
          pageSize={data.meta.limit}
          onChange={setPage}
          className="mt-4 text-center"
        />
      )}
    </div>
  );
};
```

#### Item 특화 기능

- **재고 증감 시각화**: ▲(증가) / ▼(감소) 아이콘
- **색상 구분**: 증가(초록색), 감소(빨간색)
- **변동량 표시**: "+15개 (100 → 115)" 형식

---

### 4. 공통 컴포넌트: 변경 이력 아이템

**파일**: `src/components/common/ChangeHistoryItem.tsx`

Order/Demo에서 공통으로 사용하는 개별 이력 아이템.

```tsx
interface ChangeHistoryItemProps {
  item: ChangeHistoryItem;
}

const ChangeHistoryItem: React.FC<ChangeHistoryItemProps> = ({ item }) => {
  const {
    action,
    field,
    fieldLabel,
    oldValue,
    newValue,
    userName,
    userEmail,
    accessLevel,
    createdAt,
    remarks,
  } = item;

  return (
    <div className="mb-4">
      {/* 액션 타입 + 시간 */}
      <div className="flex items-center gap-2 mb-2">
        <Tag color={getActionColor(action)}>
          {getActionLabel(action)}
        </Tag>
        <span className="text-gray-500 text-sm">
          {formatDateTime(createdAt)}
        </span>
      </div>

      {/* 변경 내용 */}
      {fieldLabel && (
        <div className="text-sm mb-2">
          <span className="font-semibold">{fieldLabel}</span> 변경:
          <div className="ml-4 mt-1">
            {oldValue && (
              <div className="text-gray-500">
                <span className="font-medium">이전:</span>{' '}
                {formatValue(oldValue, field)}
              </div>
            )}
            {newValue && (
              <div className="text-blue-600">
                <span className="font-medium">변경:</span>{' '}
                {formatValue(newValue, field)}
              </div>
            )}
          </div>
        </div>
      )}

      {/* create/delete 액션 처리 */}
      {!fieldLabel && action === 'create' && (
        <div className="text-sm text-gray-600">새로 생성됨</div>
      )}
      {!fieldLabel && action === 'delete' && (
        <div className="text-sm text-gray-600">삭제됨</div>
      )}

      {/* 작업자 정보 */}
      <div className="flex items-center gap-2 text-sm text-gray-600">
        <UserOutlined />
        <span>{userName}</span>
        <Tag color={getAccessLevelColor(accessLevel)}>
          {getAccessLevelLabel(accessLevel)}
        </Tag>
      </div>

      {/* 비고 */}
      {remarks && (
        <div className="mt-2 text-sm text-gray-600 bg-gray-50 p-2 rounded">
          💬 {remarks}
        </div>
      )}
    </div>
  );
};

export default ChangeHistoryItem;
```

---

## API 통신

### 1. React Query 훅

**파일**: `src/hooks/useChangeHistory.ts`

```tsx
import { useQuery } from '@tanstack/react-query';
import axios from 'axios';
import type { ChangeHistoryResponse } from '@/types/change-history';

interface ChangeHistoryParams {
  page?: number;
  limit?: number;
  action?: string | null;
}

// Order 변경 이력 조회
export const useOrderChangeHistory = (
  orderId: number,
  params: ChangeHistoryParams = {}
) => {
  return useQuery<ChangeHistoryResponse>({
    queryKey: ['orderChangeHistory', orderId, params],
    queryFn: async () => {
      const { data } = await axios.get(
        `/change-history/order/${orderId}`,
        { params }
      );
      return data;
    },
    enabled: !!orderId,
  });
};

// Demo 변경 이력 조회
export const useDemoChangeHistory = (
  demoId: number,
  params: ChangeHistoryParams = {}
) => {
  return useQuery<ChangeHistoryResponse>({
    queryKey: ['demoChangeHistory', demoId, params],
    queryFn: async () => {
      const { data } = await axios.get(
        `/change-history/demo/${demoId}`,
        { params }
      );
      return data;
    },
    enabled: !!demoId,
  });
};

// Item 재고 변동 이력 조회
export const useItemQuantityHistory = (
  itemId: number,
  params: ChangeHistoryParams = {}
) => {
  return useQuery<ChangeHistoryResponse>({
    queryKey: ['itemQuantityHistory', itemId, params],
    queryFn: async () => {
      const { data } = await axios.get(
        `/change-history/item/${itemId}`,
        { params }
      );
      return data;
    },
    enabled: !!itemId,
  });
};
```

#### 주요 특징

- **React Query 캐싱**: 동일 요청 시 캐시 사용
- **자동 재시도**: 네트워크 에러 시 자동 재시도
- **조건부 활성화**: `enabled` 옵션으로 유효한 ID일 때만 호출

---

### 2. TypeScript 타입 정의

**파일**: `src/types/change-history.ts`

```tsx
export interface ChangeHistoryItem {
  id: number;
  action: 'create' | 'update' | 'status_change' | 'delete' | 'quantity_change';
  field: string | null;
  fieldLabel: string | null;
  oldValue: Record<string, any> | null;
  newValue: Record<string, any> | null;
  userName: string;
  userEmail: string;
  accessLevel: 'user' | 'moderator' | 'admin';
  remarks: string | null;
  createdAt: string; // ISO8601 format
}

export interface ChangeHistoryMeta {
  total: number;
  page: number;
  limit: number;
  totalPages: number;
}

export interface ChangeHistoryResponse {
  data: ChangeHistoryItem[];
  meta: ChangeHistoryMeta;
}
```

---

## UI/UX 상세

### 1. 유틸리티 함수

**파일**: `src/utils/changeHistory.ts`

```tsx
import type { ChangeHistoryItem } from '@/types/change-history';
import dayjs from 'dayjs';

/**
 * 액션 타입별 색상 반환
 */
export const getActionColor = (action: string): string => {
  switch (action) {
    case 'create':
      return 'green';
    case 'update':
      return 'blue';
    case 'status_change':
      return 'purple';
    case 'delete':
      return 'red';
    case 'quantity_change':
      return 'orange';
    default:
      return 'default';
  }
};

/**
 * 액션 타입별 아이콘 반환
 */
export const getActionIcon = (action: string): React.ReactNode => {
  switch (action) {
    case 'create':
      return <PlusCircleOutlined />;
    case 'update':
      return <EditOutlined />;
    case 'status_change':
      return <SwapOutlined />;
    case 'delete':
      return <DeleteOutlined />;
    case 'quantity_change':
      return <StockOutlined />;
    default:
      return <InfoCircleOutlined />;
  }
};

/**
 * 액션 타입별 한글 라벨 반환
 */
export const getActionLabel = (action: string): string => {
  switch (action) {
    case 'create':
      return '생성';
    case 'update':
      return '수정';
    case 'status_change':
      return '상태 변경';
    case 'delete':
      return '삭제';
    case 'quantity_change':
      return '재고 변동';
    default:
      return action;
  }
};

/**
 * 권한별 색상 반환
 */
export const getAccessLevelColor = (level: string): string => {
  switch (level) {
    case 'admin':
      return 'red';
    case 'moderator':
      return 'blue';
    case 'user':
      return 'default';
    default:
      return 'default';
  }
};

/**
 * 권한별 한글 라벨 반환
 */
export const getAccessLevelLabel = (level: string): string => {
  switch (level) {
    case 'admin':
      return '관리자';
    case 'moderator':
      return '운영자';
    case 'user':
      return '일반';
    default:
      return level;
  }
};

/**
 * 값 포맷팅 (oldValue/newValue 객체에서 값 추출 및 표시)
 */
export const formatValue = (
  valueObj: Record<string, any> | null,
  field: string | null
): string => {
  if (!valueObj || !field) return '-';

  const value = valueObj[field];

  if (value === null || value === undefined) return '-';

  // 배열인 경우
  if (Array.isArray(value)) {
    return `${value.length}개 항목`;
  }

  // 객체인 경우
  if (typeof value === 'object') {
    return JSON.stringify(value, null, 2);
  }

  // 날짜 필드인 경우
  if (field.includes('Date') || field.includes('Time')) {
    return formatDateTime(value);
  }

  return String(value);
};

/**
 * 날짜/시간 포맷팅
 */
export const formatDateTime = (isoString: string): string => {
  return dayjs(isoString).format('YYYY-MM-DD HH:mm:ss');
};

/**
 * 재고 변동량 포맷팅
 * 예: "+15개 (100 → 115)" or "-10개 (100 → 90)"
 */
export const formatQuantityChange = (item: ChangeHistoryItem): string => {
  const oldQty = item.oldValue?.itemQuantity || 0;
  const newQty = item.newValue?.itemQuantity || 0;
  const diff = newQty - oldQty;

  return `${diff > 0 ? '+' : ''}${diff}개 (${oldQty} → ${newQty})`;
};

/**
 * 재고 변동에 따른 색상 반환
 */
export const getQuantityChangeColor = (item: ChangeHistoryItem): string => {
  const oldQty = item.oldValue?.itemQuantity || 0;
  const newQty = item.newValue?.itemQuantity || 0;

  return newQty > oldQty ? 'green' : 'red';
};

/**
 * 재고 변동에 따른 텍스트 색상 클래스 반환
 */
export const getQuantityChangeTextColor = (item: ChangeHistoryItem): string => {
  const oldQty = item.oldValue?.itemQuantity || 0;
  const newQty = item.newValue?.itemQuantity || 0;

  return newQty > oldQty ? 'text-green-600' : 'text-red-600';
};

/**
 * 재고 변동 아이콘 반환
 */
export const getQuantityChangeIcon = (item: ChangeHistoryItem): React.ReactNode => {
  const oldQty = item.oldValue?.itemQuantity || 0;
  const newQty = item.newValue?.itemQuantity || 0;

  return newQty > oldQty ? (
    <span className="text-green-600 font-bold">▲</span>
  ) : (
    <span className="text-red-600 font-bold">▼</span>
  );
};
```

---

### 2. 색상 가이드

#### 액션별 색상

| Action | 색상 | Ant Design Color | 의미 |
|--------|------|------------------|------|
| `create` | 초록색 | `green` | 새로 생성 |
| `update` | 파란색 | `blue` | 기존 정보 수정 |
| `status_change` | 보라색 | `purple` | 상태 변경 |
| `delete` | 빨간색 | `red` | 삭제됨 |
| `quantity_change` | 주황색 | `orange` | 재고 증감 |

#### 권한별 배지 색상

| Access Level | 색상 | Ant Design Color |
|--------------|------|------------------|
| `admin` | 빨간색 | `red` |
| `moderator` | 파란색 | `blue` |
| `user` | 회색 | `default` |

#### 재고 증감 색상

| 상황 | 색상 | Tailwind Class |
|------|------|----------------|
| 재고 증가 | 초록색 | `text-green-600` |
| 재고 감소 | 빨간색 | `text-red-600` |

---

### 3. UX 플로우

#### 기본 상태 (접힘)

```
┌──────────────────────────────────────┐
│ 📜 변경 이력 (총 15건)         [펼치기▼] │
└──────────────────────────────────────┘
```

- 섹션 제목만 표시
- 총 건수 표시
- "펼치기" 버튼 제공

#### 펼쳤을 때

```
┌──────────────────────────────────────┐
│ 📜 변경 이력 (총 15건)         [접기▲]  │
├──────────────────────────────────────┤
│ [전체] [생성] [수정] [상태변경] [삭제]  │ ← 필터
├──────────────────────────────────────┤
│ ● 상태 변경        2025-12-22 10:30    │
│   상태 변경:                           │
│   이전: 요청                           │
│   변경: 승인                           │
│   👤 홍길동 [운영자]                   │
├──────────────────────────────────────┤
│ ● 수정            2025-12-21 14:20    │
│   배송 주소 변경:                      │
│   이전: 서울시 강남구 ...              │
│   변경: 서울시 서초구 ...              │
│   👤 김철수 [관리자]                   │
│   💬 고객 요청으로 주소 변경            │
├──────────────────────────────────────┤
│          ... 더 많은 이력 ...          │
├──────────────────────────────────────┤
│        [1] 2 3 4 5 >                  │ ← 페이지네이션
└──────────────────────────────────────┘
```

- 필터 버튼 표시
- 타임라인 형식 목록
- 페이지네이션 (10개씩)

---

### 4. 성능 최적화

#### React Query 캐싱

```tsx
// 5분간 캐시 유지
useQuery({
  queryKey: ['orderChangeHistory', orderId, params],
  queryFn: fetchOrderChangeHistory,
  staleTime: 5 * 60 * 1000, // 5분
  cacheTime: 10 * 60 * 1000, // 10분
});
```

#### 조건부 렌더링

```tsx
// 접혀있을 때는 API 호출 안 함
const { data } = useOrderChangeHistory(orderId, {
  enabled: isExpanded, // 펼쳤을 때만 호출
});
```

#### 페이지네이션

- 한 번에 10개씩만 로드
- 무한 스크롤 대신 페이지네이션 사용 (서버 부하 감소)

---

## 구현 우선순위

### Phase 1: 기본 기능 구현 (필수) 🎯

#### 1단계: 공통 인프라

- [ ] TypeScript 타입 정의 (`src/types/change-history.ts`)
- [ ] 유틸리티 함수 (`src/utils/changeHistory.ts`)
- [ ] React Query 훅 (`src/hooks/useChangeHistory.ts`)

#### 2단계: 컴포넌트 개발

- [ ] 공통 컴포넌트: `ChangeHistoryItem.tsx`
- [ ] Order 변경 이력: `OrderChangeHistory.tsx`
- [ ] Demo 변경 이력: `DemoChangeHistory.tsx`
- [ ] Item 재고 이력: `ItemQuantityHistory.tsx`

#### 3단계: 페이지 통합

- [ ] Order 상세 페이지에 `OrderChangeHistory` 추가
- [ ] Demo 상세 페이지에 `DemoChangeHistory` 추가
- [ ] Item 상세 페이지에 `ItemQuantityHistory` 추가

#### 4단계: 테스트 및 최적화

- [ ] 각 페이지에서 정상 작동 확인
- [ ] 필터링 기능 테스트
- [ ] 페이지네이션 테스트
- [ ] 성능 최적화 (캐싱 확인)

---

### Phase 2: 고급 기능 (선택) 🚀

> **⚠️ SSE 실시간 알림 기능 임시 비활성화됨 (2025-12-22)**
> - SSE 기능은 구현되어 있으나 백엔드 API 준비 전까지 주석 처리됨
> - 활성화 필요 시: 각 컴포넌트에서 `useChangeHistorySSE` 훅 주석 해제
> - 관련 파일:
>   - `src/hooks/useChangeHistorySSE.ts`
>   - `src/components/orderRecord/OrderChangeHistory.tsx` (11, 37라인)
>   - `src/components/demonstration/DemoChangeHistory.tsx` (11, 37라인)
>   - `src/components/item/ItemQuantityHistory.tsx` (11, 39라인)

#### SSE 실시간 알림

**파일**: `src/hooks/useChangeHistorySSE.ts`

```tsx
import { useEffect } from 'react';
import { useQueryClient } from '@tanstack/react-query';
import { message } from 'antd';
import type { ChangeHistoryItem, ChangeHistoryResponse } from '@/types/change-history';

export const useChangeHistorySSE = (
  type: 'order' | 'demo' | 'item',
  id: number,
  enabled: boolean = true
) => {
  const queryClient = useQueryClient();
  const accessToken = useAuthStore((state) => state.accessToken);

  useEffect(() => {
    if (!enabled || !accessToken) return;

    const eventSource = new EventSource(
      `${process.env.NEXT_PUBLIC_API_URL}/change-history/${type}/${id}/stream`,
      {
        headers: {
          Authorization: `Bearer ${accessToken}`,
        },
      }
    );

    // 변경 이벤트 수신
    eventSource.addEventListener('change', (event) => {
      const newChange: ChangeHistoryItem = JSON.parse(event.data);

      // React Query 캐시 업데이트
      queryClient.setQueryData<ChangeHistoryResponse>(
        [`${type}ChangeHistory`, id],
        (old) => {
          if (!old) return old;

          return {
            ...old,
            data: [newChange, ...old.data],
            meta: {
              ...old.meta,
              total: old.meta.total + 1,
            },
          };
        }
      );

      // 토스트 알림
      message.info(
        `${newChange.userName}님이 ${getActionLabel(newChange.action)}했습니다`,
        3
      );
    });

    // Heartbeat (연결 유지 확인)
    eventSource.addEventListener('heartbeat', (event) => {
      console.log('SSE 연결 유지 중...', event.data);
    });

    // 에러 처리
    eventSource.onerror = (error) => {
      console.error('SSE 연결 오류:', error);
      eventSource.close();

      // 토큰 만료 시 재연결 로직 추가 가능
    };

    return () => {
      eventSource.close();
    };
  }, [type, id, enabled, accessToken, queryClient]);
};
```

**사용 예시:**

```tsx
// OrderChangeHistory.tsx

const OrderChangeHistory: React.FC<OrderChangeHistoryProps> = ({ orderId }) => {
  const [isExpanded, setIsExpanded] = useState(false);

  // SSE 실시간 알림 (펼쳤을 때만 활성화)
  useChangeHistorySSE('order', orderId, isExpanded);

  // ... 나머지 로직
};
```

#### 기타 고급 기능

- [ ] 엑셀 내보내기 (변경 이력 전체 다운로드)
- [ ] 변경 이력 검색 기능 (작업자, 날짜 범위 등)
- [ ] 변경 이력 상세 모달 (클릭 시 전체 정보 표시)
- [ ] Diff 뷰어 (변경 전/후 코드 비교 스타일)

---

## 디렉토리 구조

```
src/
├── types/
│   └── change-history.ts                # TypeScript 타입 정의
│
├── hooks/
│   ├── useChangeHistory.ts              # React Query 훅 (Phase 1)
│   └── useChangeHistorySSE.ts           # SSE 실시간 알림 훅 (Phase 2)
│
├── utils/
│   └── changeHistory.ts                 # 유틸리티 함수 (색상, 포맷팅 등)
│
├── components/
│   ├── common/
│   │   └── ChangeHistoryItem.tsx        # Order/Demo 공통 아이템 컴포넌트
│   │
│   ├── order/
│   │   └── OrderChangeHistory.tsx       # Order 변경 이력 컴포넌트
│   │
│   ├── demo/
│   │   └── DemoChangeHistory.tsx        # Demo 변경 이력 컴포넌트
│   │
│   └── item/
│       └── ItemQuantityHistory.tsx      # Item 재고 변동 이력 컴포넌트
│
└── app/
    ├── orderRecord/
    │   └── [id]/
    │       └── page.tsx                 # ← OrderChangeHistory 추가
    │
    ├── demoRecord/
    │   └── [id]/
    │       └── page.tsx                 # ← DemoChangeHistory 추가
    │
    └── item/
        └── [id]/
            └── page.tsx                 # ← ItemQuantityHistory 추가
```

---

## 참고 사항

### 1. 기존 코드 변경 최소화

- Order/Demo/Item 상세 페이지에 **단일 컴포넌트만 추가**
- 기존 로직 변경 없음
- 레이아웃 구조 유지

### 2. 일관성 유지

- 세 페이지 모두 동일한 UX 패턴 사용
- 색상 가이드 준수 (프로젝트 컬러 시스템과 통일)
- Ant Design 컴포넌트 사용 (Timeline, Tag, Pagination 등)

### 3. 접근성

- 키보드 네비게이션 지원 (Ant Design 기본 제공)
- 스크린 리더 고려 (`aria-label` 추가)

### 4. 에러 처리

```tsx
const { data, isLoading, isError, error } = useOrderChangeHistory(orderId);

if (isError) {
  return (
    <Alert
      type="error"
      message="변경 이력을 불러올 수 없습니다"
      description={error.message}
    />
  );
}
```

### 5. 빈 상태 처리

```tsx
{data?.data.length === 0 && (
  <Empty
    description="변경 이력이 없습니다"
    image={Empty.PRESENTED_IMAGE_SIMPLE}
  />
)}
```

---

## 업데이트 필수 사항

이 기능 구현 후 다음 문서를 반드시 업데이트해야 함:

1. **CHANGELOG.md**: 변경 이력 표시 기능 추가
2. **/update 페이지** (`src/app/update/page.tsx`): 사용자 공지
3. **/docs 폴더**: 비즈니스 로직 문서 업데이트

---

## 문의

- 프론트엔드 구현: 프론트엔드 개발팀
- 백엔드 API: 백엔드 개발팀
- 기획 수정: PM
